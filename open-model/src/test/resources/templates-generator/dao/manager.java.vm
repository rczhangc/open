package ${package.Service};

#set( $bo = ${entity} )
#set( $do = $bo.replace('BO','DO') )
import ${package.Entity}.${entity};
import ${package.Mapper}.entity.${do};
import ${superServiceClassPackage};

/**
 * $!{table.comment}服务类
 *
 * @author ${author}
 */
#if(${kotlin})
interface ${table.serviceName} : ${superServiceClass}<${do}>
#else
public interface ${table.serviceName} extends ${superServiceClass}<${do}> {

}
#end